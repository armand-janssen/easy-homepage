version: "3.9"
services:
  easy-links:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - ./icons:/app/public/icons
      - ./data:/data
    environment:
      - SITES_JSON_PATH=/data/sites.json
      # - GITHUB_REPO=git@github.com:user/repo.git
      # - GITHUB_SSH_KEY=/data/github_key
      # - GITHUB_TOKEN=xxxx
